# Flask_study
pyenv local 3.11.6
pip install flask 

# 環境変数を設定（Macの場合）
export FLASK_APP=flaskr
export FLASK_ENV=development

# 環境変数を設定（Windowsの場合）
set FLASK_APP=flaskr
set FlASK_ENV=development

flask run
